<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flaker.flaker">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    // ALLOW NETWORK REQUESTS
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Flaker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA75L5OdKIdMirIibjLha3M_gZUZYtK2j8" />

        <activity
            android:name=".AuthActivity"
            android:label="Flaker"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity" />
        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="" /> -->
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RequestTestActivity" />
        <!-- <activity android:name=".MapsActivity" /> -->
        <activity android:name=".EtaActivity" />
        <activity
            android:name=".FriendsListActivity"
            android:label=""
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".RequesteeViewActivity" />
        <activity android:name=".MeetupInviteActivity" />
         <activity android:name=".TestActivity" android:label=""></activity>
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".AddFriendActivity" android:label=""></activity>
    </application>

</manifest>